window.addEventListener("DOMContentLoaded", function () {
    const saveBtn = document.getElementById("saveNoticeSettingBtn");

    saveBtn.onclick = function () {
        alert("알림 설정 저장 버튼 클릭됨");
    };
});