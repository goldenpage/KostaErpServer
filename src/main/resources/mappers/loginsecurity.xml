<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oopsw.kostaerpserver.repository.dao.LoginSecurityDAO">
  <select id="findByBId" resultType="com.oopsw.kostaerpserver.vo.LoginUser">
    SELECT
    bId,
    name,
    pw
    FROM USERINFO
    WHERE bId = #{bId}
  </select>

</mapper>